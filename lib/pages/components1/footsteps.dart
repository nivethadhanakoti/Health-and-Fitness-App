import 'package:flutter/material.dart';
import 'food.dart';

class FootstepsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Footsteps Page'),
      ),
    );
  }
}