import 'package:flutter/material.dart';
import 'food2.dart';

class FootstepsPage2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Footsteps Page'),
      ),
    );
  }
}